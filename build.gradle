apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    // Add your dependencies here
}

jar {
    // Configure JAR packaging
    manifest {
        attributes(
            'Main-Class': 'com.example.Main'
        )
    }
}
